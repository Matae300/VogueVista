import { Link } from 'react-router-dom';
import { useQuery } from '@apollo/client';

const Home = ({ authToken }) => {


  return (
    <div className="">
     
    </div>
  );
};

export default Home;